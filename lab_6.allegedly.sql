------------
-- W domu --
------------
-- PROCEDURE --

-- 1. Zdefiniuj procedurę, która wyświetli na ekranie nazwisko oraz pensję pracownika
--    o identyfikatorze podanym jako pierwszy parametr. Jeżeli nie ma takiego id to zwrócona
--    zostanie informacja a braku takiego pracownika.

;


-- 2. Zdefiniować procedurę, która zwróci liczbę zatrudnionych wszystkich pracowników.

;


-- 3. Zdefiniować procedurę, która zwróci liczbę departamentów bez pracowników.

;


-- 4. Zdefiniować procedurę, która zwróci liczbę zatrudnionych w danym departamencie podanym
--    jako pierwszy parametr typu IN. Parametr drugi typu OUT zwróci liczbę zatrudnionych.

;


-- 5. Zdefiniować procedurę, która zwróci liczbę zatrudnionych w danym departamencie na danym
--    stanowisku pracy podanym odpowiednio jako pierwszy i drugi parametr typu IN.
--    Parametr trzeci typu OUT zwróci liczbę zatrudnionych.

;


-- 6. Zdefiniuj procedurę, która dla danego departamentu wyświetli wszystkie nazwiska
--    i imiona pracowników.

;


-- 7. Zdefiniuj procedurę która jako parametr wejściowy przyjmie id managera, a parametrem
--    wyjściowym zwróci średnią zarobków osób podległych pod tego managera.

;


-- FUNCTION --

-- 1. Zdefiniować funkcję, która wycina spacje na początku i końcu zmiennej typu VARCHAR2
--    i zwraca to co zostanie.

;


-- 2. Zdefiniować funkcję, która sprawdza czy dana liczba całkowita jest parzysta
--    czy nieparzysta i zwraca wartość 'Parzysta' lub 'Nieparzysta'.

;


-- 3. Zdefiniować funkcję, która dla podanej jako parametr wejściowy daty (typ DATE) zwraca
--    dzień tygodnia w języku polskim.

;


-- 4. Zdefiniować funkcję, która będzie zwracać średnią zarobków w dziale, którego numer
--    funkcja będzie przyjmować jako parametr.

;


-- 5. Zdefiniować funkcję z trzema parametrami, która sprawdzi czy dane boki tworza trójąt,
--    a jeśli tak to obliczyć jego pole.

;


-- 6. Zdefiniować funkcję zamieniajacą wszystkie spacje podkreśleniem.

;


-- 7. Zdefiniować funkcję do odwracania stringu.

;


-- 8. Zdefiniować funckcję PESEL, która sprawdza czy liczba jest poprawnym typem CHAR o określonej
--    długości zawierający tylko cyfry od 0-9 (nie sprawdzamy poprawności funkcji PESEL).

;


-- 9. Zdefiniowac funkcje, która sprawdza czy dana liczba jest liczbą pierwszą.

;


-- 10. Zdefiniować funkcję do obliczenia wartości silnia z liczby całkowitej (zdefiniować
--     obsługę błędów w przypadku liczb mniejszych od 1 i takich, które przekroczą
--     zakres wykorzystywaneo typu.

;
